import Foundation
import PrivySDK

/// Privy wallet + login. App ID and client ID arrive from the backend team; until then this only proves the SDK links.
enum PrivyClient {
    static let appId = ""
    static let clientId = ""
    static let redirectScheme = "apeme"
}
